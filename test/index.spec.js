describe(`index`, () => {
  it.skip(`should create a test task for npm test`, async () => {
    // Arrange
    // Act
    // Assert
  });
  // TODO: grok the original intent of the commands.bats file and port tests
  // -> bats is unix-only
  // -> bats doesn't appear to run on a modern node, due to a reliance on `bon`

});
